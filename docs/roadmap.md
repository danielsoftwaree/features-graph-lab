# Планерка

Что есть и что делаем дальше — источник правды по задачам.
Хронология сделанного — в [`journal.md`](./journal.md).

## Текущее состояние

| Область | Состояние |
|---|---|
| Движок потока (`shared/lib/flow-engine`) | ✅ готов, покрыт тестом |
| React-рантайм (`shared/flow-runtime.tsx`) | ✅ готов |
| Канвас: граф + анимация (`features/canvas`) | ✅ готов |
| Кейс double-charge: логика + данные + тест | ✅ готов |
| Страница кейса (shell + мокап чекаута) | 🟡 работает, но hero — плейсхолдеры |
| Панели кейса (`features/cases/components/panels`) | 🔴 написаны, но не подключены (мёртвый код) |
| Мультикейсовость | 🔴 захардкожен 1 кейс |
| Лендинг | ✅ готов (1 доступный + 2 coming-soon) |

## Бэклог

Приоритет сверху вниз.

### Чистка — быстрые победы
- [ ] Решить судьбу `features/cases/components/panels/*`: подключить к странице кейса
      **или** удалить. Сейчас 5 компонентов нигде не импортируются, из-за чего поля
      `CaseData` (businessSituation, businessImpact, finalLesson, testResults) не рендерятся.
- [ ] Наполнить `CaseHero` реальными данными кейса вместо `WireBox`-плейсхолдеров.
- [ ] Дочистить мёртвое: пустой default-export в `root-provider.tsx`,
      неиспользуемый `edgePayloads` в рантайме, рассинхрон `BadgeVariant` в `shared/types/index.ts`.

### Фичи
- [ ] Мультикейсовость: реестр `slug → {createWorld, createHandlers}` вместо хардкода
      double-charge в `case-shell.tsx`. Это разблокирует кейсы с лендинга.
- [ ] Новые кейсы: referral-abuse, last-item-race (уже заявлены на лендинге как coming-soon).

### Надёжность
- [ ] Связать граф (`double-charge.ts`) и логику (`.logic.ts`): контракт держится
      на совпадении строковых id узлов и branch-ключей рёбер без какой-либо проверки.
- [ ] Связать `ORDER_AMOUNT` (логика) с суммами в `case-app-mockup.tsx` (сейчас захардкожены строками).
