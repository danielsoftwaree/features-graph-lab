# Планерка

Что есть и что делаем дальше — источник правды по задачам.
Хронология сделанного — в [`journal.md`](./journal.md).

## Текущее состояние

| Область | Состояние |
|---|---|
| Движок потока (`shared/lib/flow-engine`) | ✅ готов, покрыт тестом |
| React-рантайм (`shared/flow-runtime.tsx`) | ✅ готов |
| Канвас: граф + анимация (`features/canvas`) | ✅ готов |
| Кейс double-charge: логика + данные + тест | ✅ готов |
| Страница кейса (shell + мокап чекаута) | ✅ полноэкранный one-screen: реальный hero, честный тулбар, нативный зум |
| Панели кейса (`features/cases/components/panels`) | ✅ удалены — выбран single-screen дизайн (лаборатория без скролла) |
| Мультикейсовость | 🔴 захардкожен 1 кейс |
| Лендинг | ✅ готов (1 доступный + 2 coming-soon) |

## Бэклог

Приоритет сверху вниз.

### Чистка — быстрые победы
- [x] Судьба `panels/*`: **удалены** — страница осознанно one-screen (полноэкранная
      лаборатория без скролла), нарративные панели в неё не вписываются.
- [x] `CaseHero` наполнен реальными данными (title + businessSituation).
- [x] Дочистка: удалён default-export в `root-provider.tsx`, `edgePayloads` из рантайма,
      `shared/types/index.ts` (`BadgeVariant`/`Slug` не использовались). Заодно убран
      фейковый тулбар канваса (WireBox-табы + зум) → честный заголовок + нативный `<Controls/>`.
- [ ] Повисли поля `CaseData`: `businessImpact`, `testResults`, `finalLesson`, `criticalNodeId`
      больше нигде не рендерятся. Решить: оставить как авторский контент под будущие табы
      **или** удалить из типа и данных.

### Фичи
- [x] Инспектор ноды: клик по ноде → панель внутри канваса с TS-кодом хендлера,
      объяснением и блоком «почему ломается» (для нод, участвующих в баге). Ноды
      получили выделение/курсор/номер шага; граф рефитится при открытии/закрытии.
- [x] Редактируемый код в инспекторе на **CodeMirror 6**: правишь TS-код ноды →
      Run/Pay компилирует его (`new Function`, локальная песочница) и исполняет в
      движке. `guard` засеян сломанным — чинишь сам; broken/fixed тумблер убран.
      Ошибки компиляции/рантайма ловятся и показываются (баннер + строка в инспекторе).
- [ ] Режим задач (суть затеи): цель кейса + проверка результата (`charges.length === 1`)
      + уровни сложности junior/middle (разные сломанные ноды/ограничения) + «Solved!».
- [ ] Инспектор, итерация 2: сторителлинг сбоя на канвасе (в broken путь ретрая
      краснеет, нода двойного списания пульсирует) + живой payload (input→output).
      Сейчас блок «почему ломается» статичен — показывается даже после фикса.
- [ ] Мультикейсовость: реестр `slug → {createWorld, initialCode}` вместо хардкода
      double-charge в `case-shell.tsx`. Это разблокирует кейсы с лендинга.
- [ ] Новые кейсы: referral-abuse, last-item-race (уже заявлены на лендинге как coming-soon).

### Надёжность
- [ ] `double-charge.logic.ts` теперь **референс/тест-only**: рантайм исполняет
      редактируемые строки кода из нод (`INSPECTOR.code`), а типизированные хендлеры
      остались только для теста. Две версии логики могут разойтись — свести к одной
      (например, генерировать тест из строк) или явно держать как «эталонное решение».
- [ ] Связать граф (`double-charge.ts`) и логику (`.logic.ts`): контракт держится
      на совпадении строковых id узлов и branch-ключей рёбер без какой-либо проверки.
- [ ] Связать `ORDER_AMOUNT` (логика) с суммами в `case-app-mockup.tsx` (сейчас захардкожены строками).
